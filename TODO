
Flags: ✓ done, ☠ bug, ☣ missing, * improvement

  * eine Variable `it` wäre schön

  ☣ Sanitize code generating output.  Implement LaTeX output.

  * Sharing wäre echt nett

  * Zusätzliche Infos ausgeben die keinen Rechenschritt darstellen.

       `add` ist strict
       `...` ist in WHNF

  * α-Konversion als Schritt anzeigen.  Wie genau?  Wenn konvertiert
    wird dann einen Schritt mit allen Konversionen?

  ☣ Fehlermeldung bei Namenskollision beim Laden.  

  * Bei α-Konversion bessere Variablennamen erzeugen.  Nicht gut:
  
           (λz x. (λx. x) z) x
        β→
           λx0. (λx0. x0) x

  * Allow spec of colors

  * Trace in Datei, festlegen was genau getraced werden soll



  ✓ Prompt changes when loading fails.  

  ✓ Programm terminiert wenn Datei nicht gefunden.

  ✓ When interrupting with C-c, all settings are restored.

